<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="container">
  <div class="box__users">
    <% @users.each do |user| %>
    <div class="box__users_items">
        <%= render user %>
    </div>
    <% end %>
  </div>
</div>
<%= will_paginate %>